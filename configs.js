'use strict';
module.exports = {
    folder: {
        api: `${__dirname}/api/`,
        controller: `${__dirname}/controller/`,
        helpers: `${__dirname}/helpers/`,
        model: `${__dirname}/model/`
    }
};
